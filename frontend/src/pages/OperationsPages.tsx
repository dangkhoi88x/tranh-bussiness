import { useEffect, useState, type FormEvent } from 'react'
import { useNavigate, useParams } from 'react-router-dom'
import { apiRequest, ApiRequestError } from '../api/http'
import { useAuth } from '../contexts/AuthContext'

type Page<T> = { items: T[]; page: number; size: number; totalElements: number; totalPages: number; hasNext: boolean }
type OrderStatus = 'PENDING' | 'CONFIRMED' | 'PROCESSING' | 'SHIPPING' | 'DELIVERY_FAILED' | 'DELIVERED' | 'CANCELLED'
type ShipmentStatus = 'READY' | 'IN_TRANSIT' | 'DELIVERED' | 'DELIVERY_FAILED' | 'CANCELLED'
type Order = { id: string; orderCode: string; status: OrderStatus; shippingAddress: string; shippingAddressSnapshot: { recipientName: string; phone: string; province: string; district: string; ward: string; addressLine: string } | null; subtotalAmount: number; discountAmount: number; totalAmount: number; promotionCode: string | null; customDetails: { requestCode: string; requestType: string; material: string; frameName: string | null; quotedPrice: number } | null; items: OrderItem[]; createdAt: string }
type OrderItem = { id: string; productName: string; variantName: string | null; variantSku: string | null; variantMaterial: string | null; frameName: string | null; unitPrice: number; quantity: number; lineTotal: number }
type History = { id: string; fromStatus: OrderStatus; toStatus: OrderStatus; changedByName: string | null; note: string | null; createdAt: string }
type Shipment = { id: string; carrier: string; trackingCode: string; shippingFee: number; status: ShipmentStatus; shippedAt: string | null; deliveredAt: string | null; failedAt: string | null; failureReason: string | null }
type CustomOrder = { id: string; requestCode: string; type: string; widthCm: number; heightCm: number; material: string; frameId: string | null; frameName: string | null; quotedPrice: number | null; staffNote: string | null; customerNote: string | null; status: 'NEW' | 'QUOTED' | 'CONFIRMED' | 'IN_PRODUCTION' | 'COMPLETED' | 'CANCELLED'; orderCode: string | null; images: { id: string; secureUrl: string }[]; createdAt: string }
type Frame = { id: string; name: string; status: 'ACTIVE' | 'INACTIVE' }

const money = new Intl.NumberFormat('vi-VN', { style: 'currency', currency: 'VND', maximumFractionDigits: 0 })
const dateTime = new Intl.DateTimeFormat('vi-VN', { dateStyle: 'medium', timeStyle: 'short' })
const errorText = (error: unknown) => error instanceof Error ? error.message : 'Đã có lỗi xảy ra.'

function OperationsHeader({ eyebrow, title, description, action }: { eyebrow: string; title: string; description: string; action?: React.ReactNode }) { return <header className="catalog-header"><div><p className="eyebrow">{eyebrow}</p><h2>{title}</h2><p>{description}</p></div>{action}</header> }
function Notice({ value }: { value: string | null }) { return value ? <p className="catalog-message" role="status">{value}</p> : null }
function Panel({ children }: { children: React.ReactNode }) { return <section className="catalog-panel">{children}</section> }
function stateClass(status: string) { return `status status--${status.toLowerCase()}` }
function Pagination({ data, onPage }: { data: Page<unknown> | null; onPage: (page: number) => void }) { if (!data || data.totalPages <= 1) return null; return <div className="pagination"><button disabled={data.page <= 1} onClick={() => onPage(data.page - 1)}>← Trước</button><span>Trang {data.page}/{data.totalPages} · {data.totalElements} bản ghi</span><button disabled={!data.hasNext} onClick={() => onPage(data.page + 1)}>Sau →</button></div> }

export function OrderDetailPage() {
  const { orderId } = useParams(); const navigate = useNavigate(); const { hasPermission } = useAuth(); const [order, setOrder] = useState<Order | null>(null); const [history, setHistory] = useState<History[]>([]); const [shipment, setShipment] = useState<Shipment | null>(null); const [message, setMessage] = useState<string | null>(null)
  async function load() { try { const [detail, timeline] = await Promise.all([apiRequest<Order>(`/orders/${orderId}`), apiRequest<History[]>(`/orders/${orderId}/history`)]); setOrder(detail); setHistory(timeline); if (hasPermission('SHIPMENT_MANAGE')) { try { setShipment(await apiRequest<Shipment>(`/orders/${orderId}/shipment`)) } catch (error) { if (!(error instanceof ApiRequestError && error.status === 404)) setMessage(errorText(error)); else setShipment(null) } } } catch (error) { setMessage(errorText(error)) } }
  useEffect(() => { void load() }, [orderId])
  if (!order) return <section className="admin-page"><p>{message || 'Đang tải đơn hàng…'}</p><button className="ghost-button" onClick={() => navigate('/admin/orders')}>← Quay lại đơn hàng</button></section>
  return <><OperationsHeader eyebrow="ĐƠN HÀNG" title={order.orderCode} description={`${dateTime.format(new Date(order.createdAt))} · ${order.shippingAddress}`} action={<button className="ghost-button" onClick={() => navigate('/admin/orders')}>← Danh sách</button>} /><Notice value={message} /><div className="operations-grid"><div className="operations-stack"><Panel><div className="panel-heading"><div><h3>Sản phẩm trong đơn</h3><p>{order.items.length} dòng sản phẩm</p></div><span className={stateClass(order.status)}>{order.status}</span></div><table className="data-table"><thead><tr><th>Sản phẩm</th><th>Đơn giá</th><th>SL</th><th>Thành tiền</th></tr></thead><tbody>{order.items.map((item) => <tr key={item.id}><td><strong>{item.productName}</strong><small>{[item.variantName, item.variantSku, item.variantMaterial, item.frameName].filter(Boolean).join(' · ') || 'Sản phẩm tiêu chuẩn'}</small></td><td>{money.format(item.unitPrice)}</td><td>{item.quantity}</td><td>{money.format(item.lineTotal)}</td></tr>)}</tbody></table><div className="order-total"><span>Tạm tính</span><strong>{money.format(order.subtotalAmount)}</strong><span>Giảm giá</span><strong>−{money.format(order.discountAmount)}</strong><span>Tổng thanh toán</span><strong className="order-total__grand">{money.format(order.totalAmount)}</strong></div></Panel>{order.customDetails && <Panel><div className="panel-heading"><div><h3>Thông tin đơn theo yêu cầu</h3><p>{order.customDetails.requestCode} · {order.customDetails.requestType}</p></div></div><div className="detail-copy">Vật liệu: <strong>{order.customDetails.material}</strong>{order.customDetails.frameName && <> · Khung: <strong>{order.customDetails.frameName}</strong></>}</div></Panel>}<Panel><div className="panel-heading"><div><h3>Lịch sử trạng thái</h3><p>Toàn bộ thao tác thay đổi của đơn.</p></div></div><ol className="timeline">{history.map((item) => <li key={item.id}><span className="timeline__dot" /><div><strong>{item.fromStatus} → {item.toStatus}</strong><p>{item.note || 'Không có ghi chú'}</p><small>{item.changedByName || 'Hệ thống'} · {dateTime.format(new Date(item.createdAt))}</small></div></li>)}</ol>{history.length === 0 && <p className="empty-state">Chưa có lịch sử trạng thái.</p>}</Panel></div><aside className="operations-side">{hasPermission('ORDER_MANAGE') && <OrderStatusCard order={order} onSaved={() => { setMessage('Đã cập nhật trạng thái đơn.'); void load() }} onError={setMessage} />}{hasPermission('SHIPMENT_MANAGE') && <ShipmentCard order={order} shipment={shipment} onSaved={() => { setMessage('Đã cập nhật vận chuyển.'); void load() }} onError={setMessage} />}{order.shippingAddressSnapshot && <Panel><div className="panel-heading"><div><h3>Địa chỉ nhận hàng</h3></div></div><div className="detail-copy"><strong>{order.shippingAddressSnapshot.recipientName}</strong><br />{order.shippingAddressSnapshot.phone}<br />{order.shippingAddressSnapshot.addressLine}, {order.shippingAddressSnapshot.ward}, {order.shippingAddressSnapshot.district}, {order.shippingAddressSnapshot.province}</div></Panel>}</aside></div></>
}

function OrderStatusCard({ order, onSaved, onError }: { order: Order; onSaved: () => void; onError: (message: string) => void }) {
  const options = order.status === 'PENDING' ? ['CONFIRMED', 'CANCELLED'] : []
  const [status, setStatus] = useState(options[0] ?? ''); const [note, setNote] = useState(''); const [busy, setBusy] = useState(false)
  if (options.length === 0) return <Panel><div className="panel-heading"><div><h3>Trạng thái đơn</h3><p>Đơn được vận hành tiếp bởi shipment hoặc đã ở trạng thái kết thúc.</p></div></div><div className="detail-copy">Trạng thái hiện tại: <span className={stateClass(order.status)}>{order.status}</span></div></Panel>
  async function submit(event: FormEvent) { event.preventDefault(); setBusy(true); try { await apiRequest(`/orders/${order.id}/status`, { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ status, note: note || null }) }); onSaved() } catch (error) { onError(errorText(error)) } finally { setBusy(false) } }
  return <Panel><div className="panel-heading"><div><h3>Xử lý đơn</h3><p>Xác nhận đơn mới hoặc hủy trước khi giao.</p></div></div><form className="admin-form side-form" onSubmit={(event) => void submit(event)}><label>Trạng thái mới<select value={status} onChange={(event) => setStatus(event.target.value)}>{options.map((option) => <option key={option}>{option}</option>)}</select></label><label>Ghi chú<textarea rows={3} value={note} onChange={(event) => setNote(event.target.value)} /></label><button className="primary-button compact" disabled={busy}>{busy ? 'Đang cập nhật…' : 'Cập nhật đơn'}</button></form></Panel>
}

function ShipmentCard({ order, shipment, onSaved, onError }: { order: Order; shipment: Shipment | null; onSaved: () => void; onError: (message: string) => void }) {
  if (!shipment) return <CreateShipmentCard order={order} onSaved={onSaved} onError={onError} />
  return <ExistingShipmentCard order={order} shipment={shipment} onSaved={onSaved} onError={onError} />
}

// DELIVERED và DELIVERY_FAILED không đi qua PUT /shipments/{id}/status — ShipmentServiceImpl.allowed()
// chỉ cho READY -> IN_TRANSIT/CANCELLED. Hoàn tất hoặc báo giao thất bại phải gọi hai endpoint
// nguyên tử riêng (/orders/{id}/fulfillment/complete, /fulfillment/delivery-failed) vì chúng còn
// đổi Payment COD và Order cùng lúc, không chỉ mỗi Shipment.
function ExistingShipmentCard({ order, shipment, onSaved, onError }: { order: Order; shipment: Shipment; onSaved: () => void; onError: (message: string) => void }) {
  const nextStatuses: Record<ShipmentStatus, ShipmentStatus[]> = { READY: ['IN_TRANSIT', 'CANCELLED'], IN_TRANSIT: ['DELIVERED', 'DELIVERY_FAILED'], DELIVERY_FAILED: ['IN_TRANSIT'], DELIVERED: [], CANCELLED: [] }
  const choices = nextStatuses[shipment.status]; const [status, setStatus] = useState<ShipmentStatus>(choices[0] ?? shipment.status); const [note, setNote] = useState(''); const [failureReason, setFailureReason] = useState(''); const [restockConfirmed, setRestockConfirmed] = useState(false); const [busy, setBusy] = useState(false)
  async function submit(event: FormEvent) { event.preventDefault(); setBusy(true); try {
    if (status === 'DELIVERED') await apiRequest(`/orders/${order.id}/fulfillment/complete`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ note: note || null }) })
    else if (status === 'DELIVERY_FAILED') await apiRequest(`/orders/${order.id}/fulfillment/delivery-failed`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ failureReason, restockConfirmed }) })
    else await apiRequest(`/shipments/${shipment.id}/status`, { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ status, failureReason: null }) })
    onSaved()
  } catch (error) { onError(errorText(error)) } finally { setBusy(false) } }
  return <Panel><div className="panel-heading"><div><h3>Vận chuyển</h3><p>{shipment.carrier} · {shipment.trackingCode}</p></div><span className={stateClass(shipment.status)}>{shipment.status}</span></div><div className="detail-copy">Phí vận chuyển: <strong>{money.format(shipment.shippingFee)}</strong>{shipment.failureReason && <><br />Lý do lỗi: {shipment.failureReason}</>}</div>{choices.length > 0 && <form className="admin-form side-form" onSubmit={(event) => void submit(event)}><label>Trạng thái shipment<select value={status} onChange={(event) => setStatus(event.target.value as ShipmentStatus)}>{choices.map((item) => <option key={item}>{item}</option>)}</select></label>{status === 'DELIVERED' && <label>Ghi chú <small>(tuỳ chọn)</small><textarea rows={2} value={note} onChange={(event) => setNote(event.target.value)} /></label>}{status === 'DELIVERY_FAILED' && <><label>Lý do giao thất bại<textarea rows={2} value={failureReason} onChange={(event) => setFailureReason(event.target.value)} required /></label><label className="check-label"><input type="checkbox" checked={restockConfirmed} onChange={(event) => setRestockConfirmed(event.target.checked)} required /> Đã nhận lại hàng hoàn về kho</label></>}<button className="primary-button compact" disabled={busy}>{busy ? 'Đang cập nhật…' : 'Cập nhật vận chuyển'}</button></form>}</Panel>
}

function CreateShipmentCard({ order, onSaved, onError }: { order: Order; onSaved: () => void; onError: (message: string) => void }) {
  const [carrier, setCarrier] = useState(''); const [trackingCode, setTrackingCode] = useState(''); const [shippingFee, setShippingFee] = useState(''); const [busy, setBusy] = useState(false)
  if (order.status !== 'CONFIRMED') return <Panel><div className="panel-heading"><div><h3>Vận chuyển</h3><p>Shipment chỉ được tạo sau khi đơn đã CONFIRMED.</p></div></div></Panel>
  async function submit(event: FormEvent) { event.preventDefault(); setBusy(true); try { await apiRequest(`/orders/${order.id}/shipment`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ carrier, trackingCode, shippingFee: Number(shippingFee) }) }); onSaved() } catch (error) { onError(errorText(error)) } finally { setBusy(false) } }
  return <Panel><div className="panel-heading"><div><h3>Tạo vận đơn</h3><p>Đơn cần có COD đang chờ trước khi bàn giao cho vận chuyển.</p></div></div><form className="admin-form side-form" onSubmit={(event) => void submit(event)}><label>Đơn vị vận chuyển<input value={carrier} onChange={(event) => setCarrier(event.target.value)} required /></label><label>Mã vận đơn<input value={trackingCode} onChange={(event) => setTrackingCode(event.target.value)} required /></label><label>Phí vận chuyển<input type="number" min="0" step="1000" value={shippingFee} onChange={(event) => setShippingFee(event.target.value)} required /></label><button className="primary-button compact" disabled={busy}>{busy ? 'Đang tạo…' : 'Tạo shipment'}</button></form></Panel>
}

export function CustomOrdersPage() {
  const [data, setData] = useState<Page<CustomOrder> | null>(null); const [page, setPage] = useState(1); const [editing, setEditing] = useState<CustomOrder | null>(null); const [message, setMessage] = useState<string | null>(null); const [loading, setLoading] = useState(true)
  async function load() { setLoading(true); try { setData(await apiRequest<Page<CustomOrder>>(`/custom-order-requests?page=${page}&size=15`)) } catch (error) { setMessage(errorText(error)) } finally { setLoading(false) } }
  useEffect(() => { void load() }, [page])
  const editable = (status: CustomOrder['status']) => ['NEW', 'CONFIRMED', 'IN_PRODUCTION'].includes(status)
  return <><OperationsHeader eyebrow="VẬN HÀNH" title="Đặt tranh theo yêu cầu" description="Báo giá, chọn khung và theo dõi quá trình sản xuất các yêu cầu riêng." /><Notice value={message} /><Panel>{loading ? <p className="table-loading">Đang tải yêu cầu…</p> : <><table className="data-table"><thead><tr><th>Mã yêu cầu</th><th>Loại / kích thước</th><th>Báo giá</th><th>Đơn hàng</th><th>Trạng thái</th><th /></tr></thead><tbody>{data?.items.map((item) => <tr key={item.id}><td><strong>{item.requestCode}</strong><small>{dateTime.format(new Date(item.createdAt))}</small></td><td>{item.type}<small>{item.widthCm} × {item.heightCm} cm · {item.material}</small></td><td>{item.quotedPrice == null ? 'Chưa báo giá' : money.format(item.quotedPrice)}</td><td>{item.orderCode || '—'}</td><td><span className={stateClass(item.status)}>{item.status}</span></td><td className="table-actions">{editable(item.status) && <button onClick={() => setEditing(item)}>{item.status === 'NEW' ? 'Báo giá' : 'Cập nhật'}</button>}</td></tr>)}</tbody></table>{data?.items.length === 0 && <p className="empty-state">Chưa có yêu cầu đặt tranh.</p>}<Pagination data={data} onPage={setPage} /></>}</Panel>{editing && <QuoteForm item={editing} onClose={() => setEditing(null)} onSaved={() => { setEditing(null); setMessage('Đã lưu báo giá / trạng thái yêu cầu.'); void load() }} />}</>
}

function QuoteForm({ item, onClose, onSaved }: { item: CustomOrder; onClose: () => void; onSaved: () => void }) {
  const [frames, setFrames] = useState<Frame[]>([]); const next: Record<CustomOrder['status'], string[]> = { NEW: ['QUOTED'], QUOTED: [], CONFIRMED: ['IN_PRODUCTION'], IN_PRODUCTION: ['COMPLETED'], COMPLETED: [], CANCELLED: [] }; const [quotedPrice, setQuotedPrice] = useState(String(item.quotedPrice ?? '')); const [frameId, setFrameId] = useState(item.frameId ?? ''); const [staffNote, setStaffNote] = useState(item.staffNote ?? ''); const [status, setStatus] = useState(next[item.status][0] ?? item.status); const [error, setError] = useState<string | null>(null); const [busy, setBusy] = useState(false)
  useEffect(() => { void apiRequest<Frame[]>('/frames/management').then(setFrames).catch((cause) => setError(errorText(cause))) }, [])
  async function submit(event: FormEvent) { event.preventDefault(); setBusy(true); setError(null); try { await apiRequest(`/custom-order-requests/${item.id}/quote`, { method: 'PUT', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ quotedPrice: Number(quotedPrice), frameId: frameId || null, staffNote: staffNote || null, status }) }); onSaved() } catch (cause) { setError(errorText(cause)) } finally { setBusy(false) } }
  return <div className="modal-backdrop"><section className="modal" role="dialog" aria-modal="true" aria-label="Báo giá yêu cầu"><header><h3>{item.requestCode}</h3><button type="button" className="icon-button" onClick={onClose}>×</button></header><form className="admin-form" onSubmit={(event) => void submit(event)}><p className="quote-summary">{item.type} · {item.widthCm} × {item.heightCm} cm · {item.material}</p>{item.images.length > 0 && <div className="quote-images">{item.images.map((image) => <img key={image.id} src={image.secureUrl} alt="Ảnh tham khảo khách hàng" />)}</div>}<div className="form-grid"><label>Báo giá<input type="number" min="0" step="1000" value={quotedPrice} onChange={(event) => setQuotedPrice(event.target.value)} required /></label><label>Khung<select value={frameId} onChange={(event) => setFrameId(event.target.value)}><option value="">Không dùng khung</option>{frames.filter((frame) => frame.status === 'ACTIVE').map((frame) => <option key={frame.id} value={frame.id}>{frame.name}</option>)}</select></label><label>Trạng thái<select value={status} onChange={(event) => setStatus(event.target.value)}>{next[item.status].map((choice) => <option key={choice}>{choice}</option>)}</select></label></div><label>Ghi chú cho khách<textarea rows={4} value={staffNote} onChange={(event) => setStaffNote(event.target.value)} /></label>{error && <p className="form-error">{error}</p>}<footer><button type="button" className="ghost-button" onClick={onClose}>Hủy</button><button className="primary-button compact" disabled={busy}>{busy ? 'Đang lưu…' : 'Lưu xử lý'}</button></footer></form></section></div>
}
