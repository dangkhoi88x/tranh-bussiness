spring:
  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  mail:
    host: ${MAIL_HOST}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

server:
  # Chạy sau nginx: đọc X-Forwarded-For để hạn mức đếm theo IP thật của khách. Thiếu dòng
  # này thì mọi request mang cùng IP của proxy và một người sẽ khoá được tất cả.
  forward-headers-strategy: framework

logging:
  level:
    root: info

app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS}
  security:
    cookie:
      secure: ${COOKIE_SECURE:true}
    jwt:
      secret: ${JWT_SECRET}
